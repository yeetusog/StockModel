[pytest]
testpaths = tests
markers =
    unit: isolated logic, no network or disk
    integration: exercises an API route end to end with the pipeline stubbed
filterwarnings =
    ignore::DeprecationWarning
